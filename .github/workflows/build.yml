name: Build APK (Disabled - Use android-build.yml instead)

# This workflow is disabled because it doesn't build required native libraries.
# Use android-build.yml for complete builds.

on:
  # push:
  #   branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Workflow disabled
      run: |
        echo "This workflow is disabled."
        echo "Please use android-build.yml instead."
        echo "It includes all required build steps:"
        echo "  - Native ripgrep library"
        echo "  - WebChat assets"
        echo "  - Android dependencies"
        exit 1
